# GOPredict Frontend

React, TypeScript, Tailwind CSS, Axios, and Recharts dashboard for the GOPredict API.

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if the FastAPI backend is not running on `http://localhost:8000`.
